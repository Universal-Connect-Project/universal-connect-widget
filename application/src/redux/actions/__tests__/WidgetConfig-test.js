// import { createReduxActionUtils } from 'utils/Test'
// import { ActionTypes, dispatcher } from 'reduxify/actions/WidgetProfile'
//
// const { actions } = createReduxActionUtils(dispatcher)

describe('WidgetConfig placeholder', () => {
  it('should be a placeholder', () => {
    expect(true).toBe(true)
  });
});

// describe('WidgetProfile dispatcher', () => {
//   it('should call showToastNotification with the correct payload', () => {
//     expect(actions.widgetProfileLoaded({ foo: 'bar' })).toEqual({
//       type: ActionTypes.WIDGET_PROFILE_LOADED,
//       payload: { foo: 'bar' },
//     })
//   })
// })
